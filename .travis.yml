language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t ashraj/react-test -f ./client/Dockerfile.dev ./client

script:
    - docker run -e CI=true ashraj/react-test npm run test